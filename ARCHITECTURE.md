# AI 건축 도면 자동 생성 서비스 — 아키텍처 문서

> **프로젝트명:** AI Architect (가칭)
> **작성자:** Jinwoo (CTO)
> **작성일:** 2026-03-11
> **상태:** Phase 1 — PoC 준비

---

## 1. 프로젝트 개요

### 비전
"대지 정보 + 요구사항을 텍스트로 입력하면 → AI가 공간 계획(플래닝)을 하고 → 치수 포함 실제 도면을 자동 생성"

### 핵심 차별점
- 기존 도구(플래너5D, 스케치업, AutoCAD)는 사람이 수동 배치
- 우리 서비스: 텍스트만 입력하면 AI가 자동으로 공간 배치 + 도면 생성
- 타겟: 중소규모 건축주, 인테리어 디자이너, 소규모 건축사무소

---

## 2. 시스템 아키텍처

```
[사용자 입력 (텍스트)]
        ↓
[① NLP 파서] — 자연어 → 구조화된 요구사항 JSON
        ↓
[② AI 플래닝 엔진] — 요구사항 + 법규 → 공간 배치 계획 (좌표 JSON)
        ↓
[③ 도면 렌더링 엔진] — 좌표 JSON → SVG/DXF/PDF 도면
        ↓
[④ 웹 프론트엔드] — 실시간 미리보기 + 편집
```

### 2.1 NLP 파서 (자연어 해석)
- **역할:** 사용자 텍스트를 구조화된 JSON으로 변환
- **기술:** Gemini 무료 API (PoC) → 오픈소스 LLM (서비스화)
- **입력 예시:** "남향 대지 200평, 2층 단독주택, 방 4개, 거실 넓게, 주차 2대"
- **출력 예시:**
```json
{
  "site": { "area_pyeong": 200, "direction": "south", "shape": "rectangle" },
  "building": { "floors": 2, "type": "detached_house" },
  "rooms": [
    { "type": "bedroom", "count": 4 },
    { "type": "living_room", "count": 1, "preference": "large" },
    { "type": "kitchen", "count": 1 },
    { "type": "bathroom", "count": 2 },
    { "type": "parking", "capacity": 2 }
  ]
}
```

### 2.2 AI 플래닝 엔진 (공간 배치)
- **역할:** 구조화된 요구사항 → 각 공간의 좌표/치수 계산
- **방식:** 하이브리드 (룰 엔진 + AI)
  - 룰 엔진: 건축법규(건폐율/용적률), 이격거리, 일조권 체크
  - AI/알고리즘: 공간 최적 배치 (constraint satisfaction)
- **출력:** 각 공간의 x, y, width, height, floor 정보

### 2.3 도면 렌더링 엔진
- **역할:** 좌표 데이터 → 건축 도면
- **기능:**
  - 벽체 (두께 포함)
  - 창문/문 심볼
  - 치수선
  - 방 라벨
  - 축척 표시
- **출력 포맷:** SVG (웹 미리보기), DXF (AutoCAD 호환), PDF

### 2.4 웹 프론트엔드
- **기술:** Next.js + TypeScript
- **기능:**
  - 텍스트 입력 폼
  - 실시간 도면 미리보기 (SVG)
  - 도면 다운로드 (SVG/DXF/PDF)
  - (Phase 2+) 드래그 편집

---

## 3. 기술 스택

| 영역 | 기술 | 이유 |
|------|------|------|
| 프론트엔드 | Next.js + TypeScript | Valentina 전문 스택, SSR 지원 |
| 백엔드 API | Node.js + Hono | 경량, 빠름 |
| AI (PoC) | Gemini 무료 API | 비용 0원 |
| AI (서비스) | 오픈소스 LLM + Ollama | API 비용 제거 |
| 도면 렌더링 | SVG.js (웹) + ezdxf (DXF) | 웹 호환 + AutoCAD 호환 |
| DB | PostgreSQL | 프로젝트/도면 저장 |
| 배포 | Vercel (프론트) + Docker (백엔드) | 빠른 배포 |

---

## 4. Phase 1 — PoC (2~3주)

### 목표
텍스트 입력 → 단독주택 1층 평면도 SVG 출력

### 범위
- 단독주택 1층만
- 직사각형 대지만
- 기본 공간: 거실, 주방, 침실, 화장실, 현관
- SVG 출력 (DXF는 Phase 2)
- 건축법규: 건폐율만 (기본 60%)

### 업무 분장

| 담당 | 업무 | 산출물 |
|------|------|--------|
| Jinwoo (CTO) | AI 플래닝 엔진 설계, 프롬프트 엔지니어링, 아키텍처 | 프롬프트, API 설계 문서 |
| Valentina (개발) | 웹 프론트엔드, 백엔드 API, SVG 렌더링 | 웹앱 코드 |
| Nina (QA) | 테스트 시나리오, AI 출력 검증 기준, 품질 체크리스트 | 테스트 문서 |

---

## 5. 데이터 플로우 상세

```
사용자: "남향 대지 60평, 1층 단독주택, 방3 거실 주방 화장실2 주차1"
    ↓
NLP 파서 → { site: {area:60, dir:"south"}, rooms: [...] }
    ↓
플래닝 엔진 → { walls: [...], rooms: [{id:"living", x:0, y:0, w:6000, h:5000}, ...] }
    ↓
렌더링 엔진 → SVG 도면 (벽체, 치수선, 라벨 포함)
    ↓
웹 브라우저에서 미리보기 + 다운로드
```

---

## 6. 리스크 & 대응

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| AI 출력 불일관 | 높음 | 룰 엔진으로 보정, 시드 고정 |
| 건축법규 정확도 | 높음 | Phase 1은 기본만, 전문가 검수 |
| 도면 정밀도 부족 | 중간 | 밀리미터 단위 좌표계 사용 |
| Gemini API 제한 | 낮음 | 무료 티어 내 충분, 폴백 준비 |

---

*이 문서는 프로젝트 진행에 따라 업데이트됩니다.*
